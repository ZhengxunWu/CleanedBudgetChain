import React, { Component, useState } from "react";
class Show extends React.Component {
  
  render() {
    //summary=campaign.methods.getSummary().call()
    return <textarea> {this.props.name}</textarea>

  }
}